package main

import "math"

const PLAYER_SIDE int = 0
const ENEMY_SIDE int = 1

const DEFAULT_HOMEBASE_HEALTH int = 1500
const DEFAULT_BASE_COUNT int = 2
const DEFAULT_BASE_OFFSET_BUFFER float32 = 2.0
const DEFAULT_TICKS_PER_BOUNCER_RESPAWN int = 90
const DEFAULT_TICKS_PER_SHIELD_REGEN int = 40
const DEFAULT_MAX_BOUNCERS int = 8
const DEFAULT_FIRE_DELAY int = 80

const RADIAN float32 = (math.Pi / 180)
